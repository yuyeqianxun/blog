# instance of
```js
function myInstance(left,right){
    left = left.__proto__;
    right = right.prototype;
    while(true){
        if(left===right){
            return true
        }else if(left===null){
            return false
        }else{
            left = left.__proto__
        }
    }
}
```

# Promise